<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Holo.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!--<item name="android:actionBarStyle">@style/MyActionBar</item>-->
    </style>
    
    <!-- ActionBar背景色
    <style name="MyActionBar" parent="@android:style/Widget.Holo.Light.ActionBar">  
        <item name="android:background">#A0A0F0</item>  
    </style> 
    -->

	<style name="ProgressBar_Mini" parent="@android:style/Widget.Holo.Light.ProgressBar.Horizontal">
        <item name="android:maxHeight">2dp</item>
        <item name="android:minHeight">2dp</item>
        <item name="android:progressDrawable">@drawable/progress_bar</item>
    </style>

    <drawable name="trans_background_green">#A8F8</drawable>

	<drawable name="trans_background_normal">#DFFF</drawable>
	<drawable name="trans_background_pressed">#ACFC</drawable>

	<drawable name="background_normal">#FFF</drawable>
	<drawable name="background_pressed">#CFC</drawable>
	
</resources>
